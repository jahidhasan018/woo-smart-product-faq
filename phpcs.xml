<?xml version="1.0"?>
<ruleset name="WooSmartFaq">
    <description>WooCommerce Smart FAQ — WordPress Coding Standards</description>

    <!-- Scan these files/directories -->
    <file>woo-smart-product-faq.php</file>
    <file>uninstall.php</file>
    <file>src/php/</file>
    <file>templates/</file>

    <!-- Exclude build artifacts, vendor, and node_modules -->
    <exclude-pattern>*/vendor/*</exclude-pattern>
    <exclude-pattern>*/node_modules/*</exclude-pattern>
    <exclude-pattern>*/assets/*</exclude-pattern>
    <exclude-pattern>*/tests/*</exclude-pattern>

    <!-- Use WordPress-Extra (includes WordPress-Core) and WordPress-Docs -->
    <rule ref="WordPress-Extra"/>
    <rule ref="WordPress-Docs"/>

    <!-- PHP version compatibility -->
    <config name="minimum_supported_wp_version" value="6.4"/>
    <config name="testVersion" value="8.1-"/>

    <!-- Plugin-specific text domain -->
    <rule ref="WordPress.WP.I18n">
        <properties>
            <property name="text_domain" type="array">
                <element value="woo-smart-product-faq"/>
            </property>
        </properties>
    </rule>

    <!-- Allow short array syntax [] — preferred over long array() for PSR-4 OOP code -->
    <rule ref="Generic.Arrays.DisallowShortArraySyntax.Found">
        <severity>0</severity>
    </rule>
    <rule ref="Universal.Arrays.DisallowShortArraySyntax">
        <severity>0</severity>
    </rule>

    <!-- PSR-4 requires PascalCase filenames — disable the lowercase-filename rule for src/php/ -->
    <rule ref="WordPress.Files.FileName">
        <properties>
            <property name="strict_class_file_names" value="false"/>
        </properties>
        <exclude-pattern>*/src/php/*</exclude-pattern>
    </rule>
</ruleset>
